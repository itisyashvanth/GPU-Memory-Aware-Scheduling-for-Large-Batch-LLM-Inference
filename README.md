# GPU-Memory-Aware-Scheduling-for-Large-Batch-LLM-Inference
Companies run Al chatbots and assistants (like ChatGPT) on powerful cloud servers with GPUs. To serve many users at once cheaply, they bundle many people's requests together and process them as a batch - like a bus picking up multiple passengers instead of running a separate car for each one.
